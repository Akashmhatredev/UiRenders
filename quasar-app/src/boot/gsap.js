import { gsap } from "gsap";
// Optional: Import plugins you need
import { ScrollTrigger } from "gsap/ScrollTrigger";
// Register plugins
gsap.registerPlugin(ScrollTrigger);

export default ({ app }, inject) => {};
